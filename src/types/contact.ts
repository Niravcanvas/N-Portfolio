// Client-safe domain type, derived from the Mongoose schema (type-only import,
// so nothing server-only is pulled into client bundles).
export type { ContactMessage } from "@/server/db/schemas/contactMessage";
